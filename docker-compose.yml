name: ai_app

services:
  mysql:
    image: mysql:8.0
    container_name: ai_app_mysql
    restart: always
    ports:
      - '3307:3306'
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-123456}
      MYSQL_DATABASE: ${DB_NAME:-ai_app}
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - app-network

  redis:
    image: redis:alpine
    container_name: ai_app_redis
    restart: always
    ports:
      - '6379:6379'
    command: redis-server --requirepass ${REDIS_PASSWORD:-123456}
    volumes:
      - redis_data:/data
    networks:
      - app-network

  backend:
    build:
      context: ./backend
      args:
        NODE_BASE_IMAGE: ${NODE_BASE_IMAGE:-node:20-alpine}
    container_name: ai_app_backend
    restart: always
    ports:
      - '4000:4000'
    environment:
      - PORT=4000
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_USER=${DB_USER:-root}
      - DB_PASSWORD=${DB_PASSWORD:-123456}
      - DB_NAME=${DB_NAME:-ai_app}
      - REDIS_URL=redis://:${REDIS_PASSWORD:-123456}@redis:6379
      - JWT_SECRET=${JWT_SECRET:-dev_secret_key_123456}
      - CORS_ORIGIN=${CORS_ORIGIN:-http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001,https://ukb88.com,https://www.ukb88.com,https://*.ukb88.com,http://ukb88.com,http://www.ukb88.com,http://*.ukb88.com}
      # Add your API keys here or in a .env file
      - DASHSCOPE_API_KEY=${DASHSCOPE_API_KEY}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - ZHIPU_API_KEY=${ZHIPU_API_KEY}
      - MOONSHOT_API_KEY=${MOONSHOT_API_KEY}
    depends_on:
      - mysql
      - redis
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
      args:
        NODE_BASE_IMAGE: ${NODE_BASE_IMAGE:-node:20-alpine}
        NGINX_BASE_IMAGE: ${NGINX_BASE_IMAGE:-nginx:stable-alpine}
    container_name: ai_app_frontend
    restart: always
    ports:
      - '3000:80'
    depends_on:
      - backend
    networks:
      - app-network

  admin:
    build:
      context: ./admin
      args:
        NODE_BASE_IMAGE: ${NODE_BASE_IMAGE:-node:20-alpine}
        NGINX_BASE_IMAGE: ${NGINX_BASE_IMAGE:-nginx:stable-alpine}
    container_name: ai_app_admin
    restart: always
    ports:
      - '3001:80'
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    name: ai_app_app-network
    driver: bridge

volumes:
  mysql_data:
  redis_data:
